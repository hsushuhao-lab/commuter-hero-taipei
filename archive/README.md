# Archive
Historical QA, recovery work, prompts, audits, and WIP are retained here for provenance only and are not production build inputs.
Production is built from `source/` + `assets/` by `scripts/build_single_file.py`.
